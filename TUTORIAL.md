# 天气预报 Web 应用：从 PRD 到部署的 Coding Agent 实战教程

## 教程目标

学员用 Coding Agent（如 Codex）完成一次完整的"需求 → 开发 → 自定义 → 发布"闭环：
拿到一份 PRD，让 Agent 从零做出一个可用的天气预报页面，再按自己的喜好自定义，
最后推送到 GitHub，并部署到 GitHub Pages / Vercel，同时用 Docker 把镜像发布到 Docker Hub。

本教程只给"思路 + 提示词"，具体实现细节由 Coding Agent 与学员在对话中完成。

---

## 阶段 0：准备（10 分钟）

确认以下账号，后面的阶段会用到（不要求现在全部配好）：

- GitHub 账号（发布代码、GitHub Pages、Actions）
- Vercel 账号（可用 GitHub 一键登录）
- Docker Hub 账号（推送镜像；注意：网页可用 GitHub 登录，但推送需要创建 Access Token）

打开 Coding Agent，给它一句总指令：

> 你是一个全栈开发助手。接下来我会分阶段给你任务，请一步步帮我完成一个天气预报 Web 应用，从写代码到部署。每个阶段先告诉我你的计划，再动手。

---

## 阶段 1：用 PRD 让 Agent 做出第一版页面

把下面的 PRD 整段复制给 Agent，加上一句"请先做出第一个可用版本"。

### 学员提示词

> 下面是产品需求文档（PRD），请根据它实现第一个可用版本：纯前端页面，数据真实可用，桌面和手机都能正常看。先搭出整体页面，再完善细节。

### PRD（可整段复制）

```markdown
# 产品需求文档：天气预报 Web 应用

## 1. 产品概述
一个纯前端的天气预报网页，数据来自公开免费接口，无需后端、无需 API Key。
目标用户之一是跑步爱好者，除基础天气外，还要提供"今天适不适合跑步"的决策建议。

## 2. 功能需求

### 2.1 当前天气（主卡片）
- 支持按城市名或经纬度搜索，支持浏览器定位；默认城市：上海
- 展示：城市与日期、大号当前温度、天气描述、最高/最低温度
- 展示三条实用建议（替代单纯的体感/湿度/风数据）：
  - 穿衣建议（按体感温度分档）
  - 是否带伞（按当前天气和降水概率）
  - 防晒建议（按紫外线指数）
- 展示日出、日落、白昼时长、月相，以及日照进度条

### 2.2 逐时与多日预报
- 未来 24 小时逐 3 小时预报（横向滑动）：时间、天气图标、温度、降水概率
- 未来 3 天预报：星期、日期、天气图标、天气描述、最高/最低温、降水概率
- 未来 24 小时气温趋势曲线，可悬停查看具体数值

### 2.3 跑者建议（跑者台）
- 跑步适宜度评分（0-100）与等级，综合：体感温度、湿度/露点、风速、降水概率、紫外线、空气质量、能见度
- 关键指标网格：空气质量、体感、湿度、风向、风速、气压、能见度、紫外线、降水概率
- 未来 24 小时最佳跑步窗口：自动挑选温度、降水、风速、紫外线最合适的时段，
  适宜度用仪表盘等视觉方式展示，时段参数用大字号直观展示
- 针对当前天气的温馨提示

### 2.4 空气质量
- 优先使用监测站实测数据（aqicn/WAQI，无需 Key）
- 显示 AQI 等级与 PM2.5、PM10、臭氧等信息，并标注数据源与口径（美标/国标、监测站/模型估算）
- 可选：支持配置和风天气 Key，获取与国内 App 一致的国标 AQI

### 2.5 动态视觉与设置
- 背景随当地时段（晨光/白昼/黄昏/夜晚）和天气变化
- 晴天有太阳光晕和云朵，雨天有雨滴粒子，雪天有飘雪，夜间有星空和真实月相
- 支持 °C/°F 切换，城市与设置本地记忆
- 适配桌面端与移动端

## 3. 技术要求
- 纯前端：HTML + CSS + JavaScript，无构建工具、无后端、无需 API Key
- 天气数据接口：https://wttr.in/<城市>?format=j1（已支持浏览器跨域）
- 空气质量：aqicn（https://api.waqi.info/feed/geo:{lat};{lon}/）与可选的和风天气
- 中文界面，天气图标使用内联 SVG

## 4. 验收标准
- 打开页面默认显示上海天气，数据真实可用
- 搜索与定位可用；跑步评分与建议合理
- 桌面与移动端布局无溢出、无空白
- 浏览器控制台无报错
```

### 与 Agent 的互动要点

第一版出来后，用对话继续打磨，例如：

> 布局太挤了，信息主次不分，请调整层级，重要信息突出，次要信息弱化
> 主卡和跑者卡的信息有重复，做一下去重
> 背景希望跟随时间和天气变化，晚上显示真实月相
> 移动端看一下，有没有溢出

原则：**你只提需求和感受，具体怎么实现交给 Agent。**

---

## 阶段 2：按自己的需求自定义

让学员想 2-3 个自己的需求，用一句话描述给 Agent。参考方向：

- 功能类：增加一周天气趋势、天气语音播报、降水概率曲线、多城市收藏
- 外观类：深色主题、某个配色、更极简/更炫酷、换图标风格
- 数据类：默认城市改为北京/你的城市、空气质量换成国标口径

### 学员提示词模板

> 在此基础上增加一个功能：<一句话描述需求>。
> 外观上我想：<一句话描述想要的风格>。
> 改完后请告诉我改了什么，并重新验证页面没有报错。

---

## 阶段 3：推送到 GitHub

确认 GitHub 已登录（学员电脑装好 GitHub CLI 或直接用网页操作）。目标：让项目成为一个独立的 GitHub 仓库。

### 学员提示词模板

> 把当前项目初始化成 git 仓库（注意只包含本项目，不要包含无关目录），提交代码，
> 然后在我的 GitHub 账号下创建公开仓库并推送。完成后告诉我仓库地址。

预期结果：一个形如 `https://github.com/<你的账号>/weather-app` 的仓库地址。

---

## 阶段 4：部署上线

任选一种，也可以两种都做：

### 方案 A：GitHub Pages

> 帮我把项目部署到 GitHub Pages，从 main 分支根目录发布，并给我线上地址。

预期结果：`https://<你的账号>.github.io/weather-app/`（或自定义域名）

### 方案 B：Vercel

> 帮我把项目部署到 Vercel 生产环境。如果本地没有 vercel 命令，先帮我安装并登录，
> 或者告诉我如何在 Vercel 网页上导入这个 GitHub 仓库一键部署。

预期结果：一个 `https://xxx.vercel.app` 的地址。

---

## 阶段 5：Docker 化并推送到 Docker Hub

目标：项目里有 Dockerfile，GitHub 每次推送代码都自动构建镜像并推到 Docker Hub。

### 第一步：让 Agent 写 Docker 相关文件

> 为项目编写 Dockerfile（用 nginx 提供静态页面）、.dockerignore，
> 并添加 GitHub Actions 工作流：每次 push 到 main 时自动构建镜像并推送到我的 Docker Hub。

### 第二步：准备 Docker Hub 凭据（学员自己操作，Agent 无法代办）

1. 注册/登录 Docker Hub，记住用户名（如 `chengzh`）
2. 创建访问令牌：Account Settings → Security → New Access Token
   **权限务必选择 Read & Write**（只读令牌无法推送镜像）
3. 到 GitHub 仓库 Settings → Secrets and variables → Actions，添加两个 Secret：
   - `DOCKERHUB_USERNAME` = Docker Hub 用户名
   - `DOCKERHUB_TOKEN` = 刚才的访问令牌

### 第三步：触发构建并验证

> 我已经配好 Docker Hub 的 Secret，请手动触发一次构建工作流，并帮我校验镜像是否出现在 Docker Hub 上。

预期结果：

- GitHub Actions 运行成功
- Docker Hub 出现 `你的用户名/weather-app`，含 `latest` 标签
- 本地验证（有 Docker 的机器）：

```bash
docker pull <你的用户名>/weather-app
docker run -d -p 8080:80 <你的用户名>/weather-app
```

---

## 常见问题速查

- **wttr.in 提示请求过于频繁（429）**：限流，稍等几分钟再试
- **Docker Hub 推送报 "insufficient scopes"**：令牌是只读权限，重新创建时选 Read & Write
- **GitHub 推送被拒（workflow 权限）**：让 Agent 运行 `gh auth setup-git` 后再推
- **线上打开是空白/数据不显示**：让 Agent 打开浏览器控制台检查 CORS 与接口报错
- **镜像查不到**：确认 Docker Hub 用户名拼写，私有仓库需登录查看

---

## 最终验收清单

- [ ] 页面能访问，真实天气数据正常显示
- [ ] 至少完成了一项自定义需求
- [ ] 项目已推送到自己的 GitHub 仓库
- [ ] 至少有 GitHub Pages 或 Vercel 一个线上地址
- [ ] Docker Hub 上能看到自己的镜像

全部完成，就实现了一次完整的"需求 → 开发 → 发布"闭环。
